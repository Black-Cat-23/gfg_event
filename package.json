{
  "name": "recruitquest-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/server\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspace=packages/types && npm run build --workspace=packages/activities && npm run build --workspace=apps/server && npm run build --workspace=apps/web"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
